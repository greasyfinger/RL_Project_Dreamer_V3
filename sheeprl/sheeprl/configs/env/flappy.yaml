defaults:
  - default
  - _self_

# Override from `default` config
id: FlappyBird-v0
mask_velocities: False

# Wrapper to be instantiated
wrapper:
  _target_: sheeprl.envs.flappy.FlappyBirdWrapper
  seed: ${seed}
